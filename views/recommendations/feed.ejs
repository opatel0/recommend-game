<%- include('../partials/meta') %>
</head>
<body>
    <%- include('../partials/header') %>
    <% for (rec of recs) { %>
        <% console.log(rec) %>
        <h2><a href="/recommendations/<%- rec._doc._id %>">Recommendation</a></h2>
        <h3>Games</h3>
        <% for (searchKey of Object.keys(rec._doc.games)) { %>
            <div><%- rec.games[searchKey].game %></div>
        <% } %>
    <% } %>
</body>
</html>